name: adwasport-api
service:
  name: adwasport-api
  type: web
  routes:
    - path: /
      port: 8000
  build:
    buildpack:
      name: python
      python_version: "3.11"
  run:
    command: uvicorn main:app --host 0.0.0.0 --port 8000